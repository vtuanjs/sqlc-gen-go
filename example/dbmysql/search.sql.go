// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: search.sql

package dbmysql

import (
	"context"
	"database/sql"
	"time"
)

const searchUsers = `-- name: SearchUsers :many
SELECT id, name, email, created_at, phone FROM users
WHERE name = ?
-- :if $2
  AND email = ?
  AND phone = ? -- :if $3
  AND EXISTS ( -- :if $5
    SELECT 1 FROM orders -- :if $5
    WHERE orders.user_id = users.id -- :if $5
      AND orders.created_at >= ? -- :if $4 -- :if $5
  ) -- :if $5
ORDER BY id ASC
`

var _searchUsersDynQ = dynCompile(searchUsers)

type SearchUsersParams struct {
	Name        string
	Email       *string
	Phone       *sql.NullString
	OrdersSince *time.Time
	HasOrders   bool
}

// The MySQL engine has no `@name` parameter syntax, so these queries use
// sqlc.arg()/sqlc.slice() while the dynamic-filter annotations still refer to
// parameters by name (`-- :if @email`).
func (q *Queries) SearchUsers(ctx context.Context, arg SearchUsersParams) ([]User, error) {

	dynQuery, dynArgs := _searchUsersDynQ.Build([]any{arg.Name, arg.Email, arg.Phone, arg.OrdersSince, arg.HasOrders})
	rows, err := q.db.QueryContext(ctx, dynQuery, dynArgs...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Email,
			&i.CreatedAt,
			&i.Phone,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchUsersByContact = `-- name: SearchUsersByContact :many
SELECT id, name, email, created_at, phone FROM users
WHERE name = ?
  AND (email = ? OR phone = ?) -- :if $2 -- :if $3
ORDER BY id ASC
`

var _searchUsersByContactDynQ = dynCompile(searchUsersByContact)

type SearchUsersByContactParams struct {
	Name  string
	Email *string
	Phone *sql.NullString
}

// Include the combined contact filter only when BOTH email AND phone are provided.
func (q *Queries) SearchUsersByContact(ctx context.Context, arg SearchUsersByContactParams) ([]User, error) {

	dynQuery, dynArgs := _searchUsersByContactDynQ.Build([]any{arg.Name, arg.Email, arg.Phone})
	rows, err := q.db.QueryContext(ctx, dynQuery, dynArgs...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Email,
			&i.CreatedAt,
			&i.Phone,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchUsersByIDs = `-- name: SearchUsersByIDs :many
SELECT id, name, email, created_at, phone FROM users
WHERE name = ?
  AND id IN (/*SLICE:ids*/?2) -- :if $2
ORDER BY id ASC
`

var _searchUsersByIDsDynQ = dynCompile(searchUsersByIDs)

type SearchUsersByIDsParams struct {
	Name string
	Ids  []int64
}

// Filter by a list of IDs. When ids is nil the condition is skipped and all
// users matching the name are returned (nil slice = inactive filter).
func (q *Queries) SearchUsersByIDs(ctx context.Context, arg SearchUsersByIDsParams) ([]User, error) {

	dynQuery, dynArgs := _searchUsersByIDsDynQ.Build([]any{arg.Name, arg.Ids})
	rows, err := q.db.QueryContext(ctx, dynQuery, dynArgs...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Email,
			&i.CreatedAt,
			&i.Phone,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchUsersOrdered = `-- name: SearchUsersOrdered :many
SELECT id, name, email, created_at, phone FROM users
WHERE name = ?
  AND email = ? -- :if $2
ORDER BY
  created_at DESC, -- :if $3
  name ASC, -- :if $4
  id ASC
`

var _searchUsersOrderedDynQ = dynCompile(searchUsersOrdered)

type SearchUsersOrderedParams struct {
	Name               string
	Email              *string
	OrderCreatedAtDesc bool
	OrderNameAsc       bool
}

func (q *Queries) SearchUsersOrdered(ctx context.Context, arg SearchUsersOrderedParams) ([]User, error) {

	dynQuery, dynArgs := _searchUsersOrderedDynQ.Build([]any{arg.Name, arg.Email, arg.OrderCreatedAtDesc, arg.OrderNameAsc})
	rows, err := q.db.QueryContext(ctx, dynQuery, dynArgs...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Email,
			&i.CreatedAt,
			&i.Phone,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchUsersOrderedByID = `-- name: SearchUsersOrderedByID :many
SELECT id, name, email, created_at, phone FROM users
WHERE name = ?
  AND email = ? -- :if $2
ORDER BY
  id ASC, -- :if $3
  id DESC -- :if $4
`

var _searchUsersOrderedByIDDynQ = dynCompile(searchUsersOrderedByID)

type SearchUsersOrderedByIDParams struct {
	Name   string
	Email  *string
	IdAsc  bool
	IdDesc bool
}

func (q *Queries) SearchUsersOrderedByID(ctx context.Context, arg SearchUsersOrderedByIDParams) ([]User, error) {

	dynQuery, dynArgs := _searchUsersOrderedByIDDynQ.Build([]any{arg.Name, arg.Email, arg.IdAsc, arg.IdDesc})
	rows, err := q.db.QueryContext(ctx, dynQuery, dynArgs...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Email,
			&i.CreatedAt,
			&i.Phone,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchUsersWithBlock = `-- name: SearchUsersWithBlock :many
SELECT id, name, email, created_at, phone FROM users
WHERE 1 = 1
  AND ( -- :if $2
    name = ? -- :if $2
    AND email = ? -- :if $2
  ) -- :if $2
ORDER BY id ASC
`

var _searchUsersWithBlockDynQ = dynCompile(searchUsersWithBlock)

type SearchUsersWithBlockParams struct {
	Name *string
}

func (q *Queries) SearchUsersWithBlock(ctx context.Context, arg SearchUsersWithBlockParams) ([]User, error) {

	dynQuery, dynArgs := _searchUsersWithBlockDynQ.Build([]any{arg.Name, arg.Name})
	rows, err := q.db.QueryContext(ctx, dynQuery, dynArgs...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Email,
			&i.CreatedAt,
			&i.Phone,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchUsersWithPhone = `-- name: SearchUsersWithPhone :many
SELECT id, name, email, created_at, phone FROM users
WHERE name = ?
  AND phone IS NOT NULL -- :if $2
ORDER BY id ASC
`

var _searchUsersWithPhoneDynQ = dynCompile(searchUsersWithPhone)

type SearchUsersWithPhoneParams struct {
	Name      string
	WithPhone bool
}

// A flag-only parameter: with_phone gates a clause that binds no value.
func (q *Queries) SearchUsersWithPhone(ctx context.Context, arg SearchUsersWithPhoneParams) ([]User, error) {

	dynQuery, dynArgs := _searchUsersWithPhoneDynQ.Build([]any{arg.Name, arg.WithPhone})
	rows, err := q.db.QueryContext(ctx, dynQuery, dynArgs...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Email,
			&i.CreatedAt,
			&i.Phone,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchUsersWithSameNameAndEmail = `-- name: SearchUsersWithSameNameAndEmail :many
SELECT id, name, email, created_at, phone FROM users
WHERE 1 = 1
  AND name = ? -- :if $2
  AND email = ? -- :if $2
ORDER BY id ASC
`

var _searchUsersWithSameNameAndEmailDynQ = dynCompile(searchUsersWithSameNameAndEmail)

type SearchUsersWithSameNameAndEmailParams struct {
	Name *string
}

// The same parameter gates (and fills) two conditions.
func (q *Queries) SearchUsersWithSameNameAndEmail(ctx context.Context, arg SearchUsersWithSameNameAndEmailParams) ([]User, error) {

	dynQuery, dynArgs := _searchUsersWithSameNameAndEmailDynQ.Build([]any{arg.Name, arg.Name})
	rows, err := q.db.QueryContext(ctx, dynQuery, dynArgs...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Email,
			&i.CreatedAt,
			&i.Phone,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchUsersWithTopStyle = `-- name: SearchUsersWithTopStyle :many
SELECT id, name, email, created_at, phone FROM users
WHERE 1 = 1
-- :if $2
  AND ( -- :if $2
    name = ? -- :if $2
    AND email = ? -- :if $2
  ) -- :if $2
ORDER BY id ASC
`

var _searchUsersWithTopStyleDynQ = dynCompile(searchUsersWithTopStyle)

type SearchUsersWithTopStyleParams struct {
	Name *string
}

func (q *Queries) SearchUsersWithTopStyle(ctx context.Context, arg SearchUsersWithTopStyleParams) ([]User, error) {

	dynQuery, dynArgs := _searchUsersWithTopStyleDynQ.Build([]any{arg.Name, arg.Name})
	rows, err := q.db.QueryContext(ctx, dynQuery, dynArgs...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Email,
			&i.CreatedAt,
			&i.Phone,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
